# AXIOM v2 — Adaptive eXecution & Import Optimization Module
# Multi-agent OS-Loader Simulation · LangGraph + Ollama/Claude
# ─────────────────────────────────────────────────────────────
# Install: pip install -r requirements.txt
# Requires: Python >= 3.10

# ── LangGraph + LangChain (agent pipeline) ────────────────────
langgraph>=1.2.0
langchain>=1.3.0

# ── LLM providers ─────────────────────────────────────────────
langchain-anthropic>=1.4.0   # Claude (needs ANTHROPIC_API_KEY)
langchain-ollama>=0.2.0      # Ollama (free, local, default)
anthropic>=0.40.0            # Anthropic SDK

# ── CLI + terminal output ──────────────────────────────────────
typer>=0.12.0                # CLI framework (axiom run / verify / experiment)
rich>=13.0.0                 # terminal tables, panels, syntax highlighting

pandas>=2.0.0
polars>=0.20.0               # Fast DataFrame alternative
ujson>=5.8.0                 # Ultra-fast JSON parsing
python-dateutil>=2.8.0       # Date utilities

fastapi>=0.110.0
uvicorn>=0.29.0